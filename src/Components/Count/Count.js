import React from 'react';

const Count = () => {
    return(
        <div className='center f3'>
            <div>Name, you have detect ? images.</div>
            <div></div>
        </div>
    );
}

export default Count;